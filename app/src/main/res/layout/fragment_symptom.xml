<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_marginStart="@dimen/activity_horizontal_margin"
    android:layout_marginEnd="@dimen/activity_horizontal_margin">

    <TextView
        android:id="@+id/textView_date"
        style="@style/DateText"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintEnd_toStartOf="@id/guideline"
        android:layout_marginEnd="40dp"/>

    <androidx.constraintlayout.widget.Guideline
        android:id="@+id/guideline"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintGuide_percent="0.5"
        android:orientation="vertical"/>

    <TextView
        android:id="@+id/textView_time"
        style="@style/DateText"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="@id/guideline"
        android:layout_marginStart="40dp"/>

    <EditText
        android:id="@+id/editText_symptom"
        style="@style/InputFields"
        android:hint="@string/hint_symptom"
        app:layout_constraintTop_toBottomOf="@id/textView_date"
        app:layout_constraintStart_toStartOf="parent"/>

    <EditText
        android:id="@+id/editText_notes"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="@string/hint_notes"
        android:gravity="top"
        android:lines="5"
        app:layout_constraintTop_toBottomOf="@id/editText_symptom"
        android:layout_marginTop="20dp"
        app:layout_constraintStart_toStartOf="parent"/>

    <ImageButton
        android:id="@+id/button_add_image"
        style="@style/SaveButton"
        android:src="@drawable/ic_photo_camera_24dp"
        android:background="@drawable/orange_rounded_corners_button"
        android:visibility="visible"
        app:layout_constraintTop_toBottomOf="@id/editText_notes"
        android:layout_marginTop="20dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <HorizontalScrollView
        android:id="@+id/scrollView_add_image"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@id/button_add_image"
        android:layout_marginTop="20dp">

        <LinearLayout
            android:id="@+id/symptom_images_container"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal">

        </LinearLayout>
    </HorizontalScrollView>

    <Button
        android:id="@+id/button_save_symptom"
        style="@style/SaveButton"
        android:visibility="visible"
        app:layout_constraintTop_toBottomOf="@id/scrollView_add_image"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>